{{!< default}}

{{> header}}

<div class="container">
    <div class="row mb-5 mt-5 ml-auto mr-auto">
        {{#foreach posts}}
            <div class="col-12 col-sm-6 col-md-4 col-lg-3 p-1 text-center">
                {{> sleeve has_tags=true}}
            </div>
        {{/foreach}}
    </div>
    {{ pagination }}
</div>
